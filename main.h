#ifndef HEADER_H
#define HEADER_H
#include <stdarg.h>
#include <stdlib.h>
#include <limits.h>
#include <unistd.h>
/**
 * struct form
int _putchar(char c);
int _printf(const char *format, ...);
int _strlen(char *s);

#endif
